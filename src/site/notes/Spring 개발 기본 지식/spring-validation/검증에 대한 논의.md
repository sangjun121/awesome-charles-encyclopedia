---
{"dg-publish":true,"permalink":"/spring/spring-validation//","dg-note-properties":{"분류":"우테코"}}
---

### 왜 모든 객체에 검증을 진행하면 좋지 않을까?

모든 객체에 대해 검증을 진행하는 것이 안전하지 않을까? 만약 검증 규칙이 문자열 5자에서 10자로 변경된다면, 모든 객체의 중복된 검증 로직을 수정해야 한다. 중복 로직은 소프트웨어의 독이다. 이를 해결하기 위해 올바른 컨벤션으로 중복 검증로직을 최소한으로 유지하는 것이 중요하다.

